{
  "name": "webrtc-demo-client",
  "version": "1.0.0",
  "description": "WebRTC P2P console client with native C++ bindings",
  "main": "client.js",
  "scripts": {
    "install": "echo Skipping native build. Run build.bat manually.",
    "start": "node client.js",
    "build": "build.bat",
    "clean": "node-gyp clean"
  },
  "dependencies": {
    "node-addon-api": "^7.1.0",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "node-gyp": "^10.0.0"
  }
}
